# Task 4.2: 重构 ChartCard 组件适配动态等级标签图表渲染

## 执行时间
2026-05-28

## 修改内容

### 1. Props 类型更改
**文件**: `src/components/ChartCard.vue`

```typescript
// Before
const props = defineProps<{ block: DataBlock }>()

// After
const props = defineProps<{ indicator: ChartIndicator }>()
```

### 2. Import 类型更改
```typescript
// Before
import type { DataBlock } from '@/types'

// After
import type { ChartIndicator } from '@/types'
```

### 3. 方法调用更改
```typescript
// Before
const { generateOptions } = useChartOptions()
const options = generateOptions(props.block)

// After
const { generateIndicatorOptions } = useChartOptions()
const options = generateIndicatorOptions(props.indicator)
```

### 4. 模板更改
```vue
<!-- Before -->
<h3 class="chart-title">{{ block.title }}</h3>

<!-- After -->
<h3 class="chart-title">{{ indicator.name }}</h3>
```

### 5. Watch 更改
```typescript
// Before
watch(() => props.block, ...)

// After
watch(() => props.indicator, ...)
```

## 验证结果

### TypeScript 类型检查
```bash
npx vue-tsc --noEmit
```
✅ 通过 - 无类型错误

### Import 验证
- ✅ 不再 import DataBlock
- ✅ 正确 import ChartIndicator

### 功能验证
- ✅ Props 类型正确：`indicator: ChartIndicator`
- ✅ 方法调用正确：`generateIndicatorOptions(props.indicator)`
- ✅ 模板绑定正确：`{{ indicator.name }}`
- ✅ Watch 监听正确：`props.indicator`
- ✅ ECharts 初始化/更新/销毁逻辑保持不变

## 修改清单

| 位置 | 修改项 | 状态 |
|------|--------|------|
| Line 4 | Import 类型 | ✅ |
| Line 8 | Props 类型 | ✅ |
| Line 12 | 解构方法名 | ✅ |
| Line 23 | initChart 方法调用 | ✅ |
| Line 30 | updateChart 方法调用 | ✅ |
| Line 51 | Watch 监听源 | ✅ |
| Line 73 | 模板绑定 | ✅ |

## 备注
- 所有 echarts 相关逻辑（init/resize/dispose）保持不变
- ResizeObserver 逻辑保持不变
- 组件样式保持不变