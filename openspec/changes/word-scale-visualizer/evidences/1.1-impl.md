# 执行证据：1.1 更新依赖：移除 xlsx，新增 jszip

**完成时间**: 2026-05-28T22:23:00+08:00
**Category**: quick

## 实现摘要

修改了 package.json 中的三处内容：将项目名从 `excel-chart-visualizer` 改为 `word-scale-visualizer`，移除 `xlsx` 依赖，新增 `jszip@^3.10.1` 依赖。由于内网 npm registry (npm.nexhome.cn) 超时，改用官方 registry (registry.npmjs.org) 完成安装。安装后 xlsx 已从 node_modules 移除，jszip 3.10.1 已正确安装。

## 变更文件清单

| 文件路径 | 操作 | 说明 |
|----------|------|------|
| package.json | 修改 | name 改为 word-scale-visualizer，dependencies 中 xlsx 替换为 jszip |
| package-lock.json | 修改 | npm install 自动更新，移除 xlsx 相关条目，新增 jszip 依赖树 |
| node_modules/xlsx/ | 删除 | npm install 自动清理 |
| node_modules/jszip/ | 新建 | npm install 自动安装 jszip@3.10.1 |

## 关键实现细节

- 内网 registry `npm.nexhome.cn` 连接超时（ETIMEDOUT 192.168.161.48:443），需使用 `--registry https://registry.npmjs.org` 参数安装
- jszip 安装版本为 3.10.1，与 package.json 中指定的 ^3.10.1 一致
- package-lock.json 已更新，后续任务可直接 `npm install` 无需额外配置

## 未完成事项

无
